typedef struct {
  byte r;
  byte g;
  byte b;
} RGB;

// Define some colors we'll use frequently
RGB white = { 255, 255, 255 };
RGB red = { 255, 0, 0 };
RGB blue = { 64, 140, 202 };
RGB green = { 0, 255, 0 };
RGB off = { 0, 0, 0 };
